<base:BaseUserControl
    x:Class="AzureAppRegistrationsManager.WinUI.Features.ApiPermissions.ApiPermissionsUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="using:AzureAppRegistrationsManager.WinUI.Features"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Padding="12" Spacing="12">
        <StackPanel Orientation="Vertical" Spacing="4">
            <Button
                IsEnabled="{x:Bind CanEdit, Mode=OneWay}"
                HorizontalAlignment="Left"
                Click="RequestResourceAccess_Click">
                <StackPanel Orientation="Horizontal">
                    <SymbolIcon Symbol="Add"/>
                    <TextBlock Text="Request Resource Access"/>
                </StackPanel>
            </Button>

            <Button
                IsEnabled="{x:Bind CanEdit, Mode=OneWay}"
                HorizontalAlignment="Left"
                Click="AddPermission_Click">
                <StackPanel Orientation="Horizontal">
                    <SymbolIcon Symbol="Add"/>
                    <TextBlock Text="Add Permission (admin only)"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <controls:DataGrid 
            AutoGenerateColumns="False"
            ItemsSource="{x:Bind ApiPermissionsSorted, Mode=OneWay}"
            HeadersVisibility="Column"
            IsReadOnly="True"
            MinHeight="300">

            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="ApplicationName" Binding="{Binding ApplicationName}" />
                <controls:DataGridTextColumn Header="Scope" Binding="{Binding Scope}" />
                <controls:DataGridTextColumn Header="ConsentType" Binding="{Binding ConsentType}" />
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </StackPanel>

</base:BaseUserControl>