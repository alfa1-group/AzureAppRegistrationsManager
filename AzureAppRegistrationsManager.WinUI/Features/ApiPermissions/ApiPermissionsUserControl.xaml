<base:BaseUserControl
    x:Class="AzureAppRegistrationsManager.WinUI.Features.ApiPermissions.ApiPermissionsUserControl"
    x:Name="self"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AzureAppRegistrationsManager.WinUI.Features.ApiPermissions"
    xmlns:base="using:AzureAppRegistrationsManager.WinUI.Features"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Padding="12" Spacing="12">
        <Button
            IsEnabled="{x:Bind CanEdit, Mode=OneWay}"
            HorizontalAlignment="Left"
            Click="AddPermission_Click">
            <StackPanel Orientation="Horizontal">
                <SymbolIcon Symbol="Add"/>
                <TextBlock Text="Add a Permission"/>
            </StackPanel>
        </Button>

        <controls:DataGrid 
            AutoGenerateColumns="False"
            ItemsSource="{x:Bind ApiPermissionsSorted, Mode=OneWay}"
            HeadersVisibility="Column"
            IsReadOnly="True"
            MinHeight="300">

            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="ApplicationName" Binding="{Binding ApplicationName}" />
                <controls:DataGridTextColumn Header="Scope" Binding="{Binding Scope}" />
                <controls:DataGridTextColumn Header="ConsentType" Binding="{Binding ConsentType}" />
                <controls:DataGridTemplateColumn>
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate x:DataType="local:ApiPermissionModel">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Button IsEnabled="false" x:Name="PermissionEditButton" FontSize="10px" Content="✏️" Tag="{x:Bind}" />
                                <Button IsEnabled="false" x:Name="PermissionDeleteButton" FontSize="10px" Content="🗑️" Tag="{x:Bind}" />
                            </StackPanel>
                            <!--<StackPanel Orientation="Horizontal" Spacing="8">
                                <Button IsEnabled="{Binding CanEdit, Mode=OneWay}" x:Name="PermissionEditButton" FontSize="10px" Content="✏️" Click="PermissionAction_Click" ToolTipService.ToolTip="Edit this permission" Tag="{x:Bind}" />
                                <Button IsEnabled="{Binding CanEdit, Mode=OneWay}" x:Name="PermissionDeleteButton" FontSize="10px" Content="🗑️" Click="PermissionAction_Click" ToolTipService.ToolTip="Delete this permission" Tag="{x:Bind}" />
                            </StackPanel>-->
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </StackPanel>

</base:BaseUserControl>