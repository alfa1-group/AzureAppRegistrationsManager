<base:BaseUserControl
    x:Class="AzureAppRegistrationsManager.WinUI.Features.ApiPermissions.ApiPermissionsUserControl"
    x:Name="self"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AzureAppRegistrationsManager.WinUI.Features.ApiPermissions"
    xmlns:base="using:AzureAppRegistrationsManager.WinUI.Features"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:converters="using:AzureAppRegistrationsManager.WinUI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Padding="12" Spacing="12">
        <Button
        x:Name="AddPermissionButton"
        IsEnabled="{x:Bind CanEdit, Mode=OneWay}"
        HorizontalAlignment="Left"
        Click="AddPermission_Click">
            <StackPanel Orientation="Horizontal">
                <SymbolIcon Symbol="Add"/>
                <TextBlock Text="Add a Permission"/>
            </StackPanel>
        </Button>

        <controls:DataGrid 
            x:Name="PermissionsDataGrid"
            AutoGenerateColumns="False"
            ItemsSource="{x:Bind ApiPermissionsSorted, Mode=OneWay}"
            HeadersVisibility="Column"
            IsReadOnly="True"
            MinHeight="300">

            <!--<controls:DataGrid.Resources>
                <converters:ConsentTypeToDisplayConverter x:Key="ConsentTypeToDisplayConverter" />
            </controls:DataGrid.Resources>-->

            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="ApplicationName" Binding="{Binding Scope.ApplicationName}" />
                <!--<controls:DataGridTextColumn Header="Admin consent display name" Binding="{Binding Scope.AdminConsentDisplayName}" />
                <controls:DataGridTextColumn Header="Admin consent description" Binding="{Binding Scope.AdminConsentDescription}" />
                <controls:DataGridTextColumn Header="User consent display name" Binding="{Binding Scope.UserConsentDisplayName}" />
                <controls:DataGridTextColumn Header="User consent description" Binding="{Binding Scope.UserConsentDescription}" />
                <controls:DataGridTextColumn Header="Consent type" Binding="{Binding Scope.Type}" />
                <controls:DataGridTextColumn Header="Enabled" Binding="{Binding Scope.IsEnabled}" />
                <controls:DataGridTemplateColumn>
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate x:DataType="local:ScopeViewModel">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Button IsEnabled="{Binding CanEdit, Mode=OneWay}" x:Name="ScopeEditButton" FontSize="10px" Content="✏️" Click="ScopeAction_Click" ToolTipService.ToolTip="Edit this scope" Tag="{x:Bind}" />
                                <Button IsEnabled="{Binding CanEdit, Mode=OneWay}" x:Name="ScopeDeleteButton" FontSize="10px" Content="🗑️" Click="ScopeAction_Click" ToolTipService.ToolTip="Delete this scope" Tag="{x:Bind}" />
                            </StackPanel>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>-->
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </StackPanel>

</base:BaseUserControl>